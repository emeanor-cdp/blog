<h1>Articles</h1>

<p>This blog has <%= Article.public_count %> articles, and it's apt to get more.</p>

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <!-- This is the manual way, replaced by the resource helper method link_to.  -->
        <!-- <a href="/articles/<%= article.id %>"> -->
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
